# ch-pd-workflow 使用指南：工具环境与 Skill 逐一说明

> 读者：使用这套 skill 干活的人（域工程师 / 机长）。配套的 README.md 讲设计思路，本指南只讲「怎么装、怎么用」。
> 范围：产品阶段（/ceo-office → /pd-review）＋ 开发阶段（/tech-spec → 合并）＋ 发布后复盘（/review）＋ 交叉机制（/prototype、/handoff）。验证（P4）与发布（P5）流程本身尚未纳入。

---

## 一、全链流程总览

| 阶段 | 命令 | 产物（写到哪） | 谁确认 |
|---|---|---|---|
| 方向判断 | `/ceo-office` | 判断结论（对话 + `./docs/` 文档） | 人 |
| 需求收敛 | `/pd-plan` | feature brief → `./docs/features/<slug>/` | 人（共识摘要确认） |
| 正式需求 | `/prd` | PRD → `./docs/features/<slug>/` | — |
| 需求评审 | `/pd-review` | 修订后的 PRD + pd-review-report | 结论=可交付才流转 |
| 小变更 | `/issue` | change request → `./docs/features/<slug>/` | 人 |
| 原型验证（交叉） | `/prototype` | 一次性原型 + verdict → `./dev/features/<slug>/prototypes/<短名>/` | 人（上手裁决） |
| 仓库初始化 | `/setup-dev` | `./dev/agents-config.md` + rule 种子（CI 种子仅 ci 模式） | 人（每仓库一次） |
| 技术方案 | `/tech-spec` | tech-spec → `./dev/features/<slug>/` | 人（共识确认） |
| 任务拆解 | `/issue-split` | 票 → `./dev/features/<slug>/issues/`（+GitLab issue） | **门②：人审票清单（执行段唯一人工门）** |
| 派发与实现 | 主会话通过宿主新开独立对话自动派发 `/implement`，并监督到合并 | worktree 分支 + 证据 + draft MR | 无需人审；质量门硬卡，BLOCKER 回修 |
| 独立评审 | `/ai-review`（由实现会话触发，默认 delegate 模式） | review-report → `./dev/features/<slug>/reviews/` | 无 BLOCKER 才转 ready |
| 合并与推进 | **主会话自动执行**（`glab mr merge` / 本地 merge，票置 done，派下一张） | — | 无需人审；仅 needs-human / 阻塞 / 无法裁决时找人 |
| 复盘 | `/review`（票全部 done 且人确认发布后手动触发） | 复盘报告 → `./docs/features/<slug>/`；经验库 → `./docs/EXPERIENCE.md` ＋ `~/.pd-workflow/general-experience.md` | 人（确认发布，审复盘结论） |
| 会话交接（交叉） | `/handoff` | 不落盘——交接提示词直接输出在当前对话，人复制到新会话 | 人（复制即交接） |

贯穿机制：`feature-slug` 全链唯一 ID；`./docs/GLOSSARY.md` 术语单源；产物驱动流转（不信「做完了」的口头汇报，看文件）。

---

## 二、工具与环境配置（使用者）

### 2.1 工具清单

| 工具 | 用途 | 安装 | 验证 | 必要性 |
|---|---|---|---|---|
| Claude Code / ZCode / Codex / WorkBuddy 等通用 agent 宿主 | skill 运行宿主（派发只要求宿主具备「新开独立对话」能力：Claude Code `Task`、ZCode `Agent`，Codex / WorkBuddy 用各自新会话 / 派发机制） | 见各宿主官方安装说明 | 宿主能启动会话 | 必需 |
| GLM 账号（bigmodel.cn，境内） | 编码 agent 模型端点 | 公司/个人开通 Coding Plan | 见 2.2 | 必需 |
| Git ≥ 2.41 | diff / worktree / 分支 | 系统包管理器 | `git --version` | 必需 |
| glab（GitLab CLI） | 建 issue / draft MR / 查 CI | 见 GitLab 官方文档 | `glab auth status` | tracker=gitlab 必需 |
| open-code-review（ocr） | AI 评审引擎 | `npm install -g @alibaba-group/open-code-review` | `ocr --version` | dev 阶段必需 |
| Node ≥ 22.6 | 仅仓库维护者构建 SKILL.md 用 | 官方安装包 | `node -v` | 维护者必需，使用者不需要 |

### 2.2 编码 agent 宿主接 GLM

宿主以 Claude Code 为例：GLM 提供 Anthropic 协议兼容端点，设环境变量直连（具体以智谱官方文档为准）：

```bash
export ANTHROPIC_BASE_URL=https://open.bigmodel.cn/api/anthropic
export ANTHROPIC_AUTH_TOKEN=<你的 GLM API key>
```

合规：GLM 端点在境内，满足数据不出境；涉密/型号数据仍不得进入任何云端模型（含 GLM）。

### 2.3 ocr 安装与端点配置（评审引擎）

```bash
npm install -g @alibaba-group/open-code-review   # 前置：Git >= 2.41
```

**默认的 `delegate` 评审模式不需要任何 LLM 端点**（ocr 只做文件筛选与规则解析，评审由宿主 agent 内新开的独立对话执行），装完 ocr 即可用。只有选择 `local` / `ci` 模式时才需要配 GLM 端点（OpenAI 协议，境内）：

```bash
ocr config set provider glm
ocr config set custom_providers.glm.url https://open.bigmodel.cn/api/paas/v4
ocr config set custom_providers.glm.protocol openai
ocr config set custom_providers.glm.model <模型名>
ocr config set custom_providers.glm.api_key <GLM API key>   # 自定义 provider 无环境变量回退，必须显式配

ocr llm test    # 连通性验证，通过才算配好
```

配置存于 `~/.opencodereview/config.json`；api_key 只进这里，不进任何入库文件。

### 2.4 GitLab CI 自动评审接入（可选增强，仅 `ocr.mode: ci` 需要）

内网 GitLab 已确认可用。四步：

1. 仓库放 `ci/ocr-review.gitlab-ci.yml`（种子模板见 `skills/product-workflow/shared/templates/gitlab-ci-ocr.yml`）；
2. 回贴脚本：从能访问 GitHub 的机器下载上游 `examples/gitlab_ci/post_review.py`，放入仓库 `ci/ocr-post-review.py`（内网 CI 不要现场 curl 外网）；
3. 项目根 `.gitlab-ci.yml` 加 `include: local: 'ci/ocr-review.gitlab-ci.yml'`；
4. GitLab 后台 Settings → CI/CD → Variables：`OCR_LLM_URL`、`OCR_LLM_AUTH_TOKEN`（掩码）、`OCR_LLM_MODEL` 必需；`GITLAB_API_TOKEN`（掩码，api scope）可选。

效果：MR 每次 push 自动评审，发现内联回贴 MR 对应行；同一 MR 串行复审；结果留 artifacts `.ocr/ocr-result.json`。

以上 1–3 步 `/setup-dev` 可代做，第 4 步必须人操作。

### 2.5 skill 安装到宿主

- **安装单元是整棵 `skills/product-workflow/` 目录**（各 SKILL.md 以相对路径引用 `../shared/templates/`，单拆一个 skill 目录会丢模板）。一条命令重建并同步三个宿主：

```bash
bash ./sync.sh    # 重新生成全部 SKILL.md，再按宿主布局分发：
                  #   Claude Code / ZCode：平铺（skill 目录 + shared）到 ~/.claude/skills/、~/.zcode/skills/
                  #   Codex：整棵嵌套到 ~/.codex/skills/product-workflow/
                  # Windows：powershell -ExecutionPolicy Bypass -File .\sync.ps1
```

- 手动方式（等价，以 Claude Code 为例）：`cp -r ~/Desktop/ch-pd-workflow/skills/product-workflow ~/.claude/skills/`。
- 重启宿主会话后，`/ceo-office` 等命令即可用。
- 宿主适配：skill frontmatter 里 `allowed-tools` 的 `Task` 是 Claude Code 的工具命名；ZCode 映射为 `Agent`，Codex / WorkBuddy 等映射为各自的新开对话机制——协议语义不变，无需改文件。宿主完全没有自动新开对话能力时按兜底执行：主会话产出自包含派发提示词，请人在新对话窗口启动；评审（`/ai-review`）的独立对话要求任何宿主都不降级。
- 仓库内 `SKILL.md` 是生成物：改了 `SKILL.md.tmpl` 或 `shared/` 后须重跑 `bash ./sync.sh`（或 `bash skills/product-workflow/scripts/build-all.sh` 再手动分发）才会在宿主里生效（维护者环境需 Node ≥ 22.6）。

---

## 三、Skill 使用说明（逐一）

### 3.1 `/setup-dev` —— 仓库初始化（每仓库一次）

- **定位**：产出 dev 阶段唯一配置 `./dev/agents-config.md`，并落评审规则种子（GitLab CI 种子仅 ci 模式落盘）。
- **默认形态**：执行与评审都在当前 agent 内完成——issue 生成后主会话通过宿主 agent 的「新开独立对话」能力自动派发实现（`dispatch: subagent`），并监督执行直到完成合并；被派发对话模型默认取宿主可用范围内经济性最高的一档（`economy`，比主会话低一档）；评审默认 `delegate`；质量门日常只跑 fast 档，full 档保留到交付前。
- **输入**：无（自动探查 git remote / glab / ocr / 测试框架）。
- **用法**：`/setup-dev`，逐项确认 tracker（gitlab 首选）、ocr 模式（**delegate 默认**；local/ci 可选）、主分支、仓库等级（A/B/C）、质量门命令、红线、轮次上限（默认 99）；GLM 端点仅在选 local/ci 时配置并现场 `ocr llm test`。
- **产出**：`./dev/agents-config.md`、`.gitignore` 加 `.worktrees/`、`.opencodereview/rule.json` 种子；选 ci 模式时另有 `ci/ocr-review.gitlab-ci.yml`。
- **下一步**：`/tech-spec`。
- **纪律**：仓库等级 A（飞行软件/涉密）会被显式警示——禁跑 `/implement`；`ocr llm test` 不通过不许落 local/ci 评审模式（delegate 不受此限）。

### 3.2 `/ceo-office` —— 方向判断

- **定位**：这件事值不值得做（服务谁、真需求吗、代价、阶段优先级、独特价值）。
- **不用**：需求已经明确要做、只差收敛细节时，直接 `/pd-plan`。
- **用法**：`请用 /ceo-office 判断：我想做 X，当前阶段值不值得做？`
- **产出**：判断结论与依据（写入 `./docs/` 相关文档）。
- **下一步**：值得做 → `/pd-plan`；不值 → 停。

### 3.3 `/pd-plan` —— 需求收敛（拷打）

- **定位**：把初步值得做的需求，经轮次制拷打（11 维度逐项归属：已确认/假设/待确认）变成 feature brief。
- **输入**：口语需求或已有 brief/issue；自动匹配 `./docs/features/` 下既有 feature-slug。
- **用法**：`请用 /pd-plan 收敛这个需求：<描述>`。AI 会分轮提问（每题带推荐答案），frontier 清空且输出共识摘要请你确认后才写文档。拷打中遇到「必须上手反应才能回答」的问题（交互感觉、状态机尴尬路径）时，该题挂 `待确认（需原型）` 不阻塞其余问题，转 `/prototype` 验证后以一行答案回来继续。
- **产出**：feature brief → `./docs/features/<slug>/<summary>-feature-brief-<日期>.md`；新术语即时回写 GLOSSARY（被淘汰的说法记「拒绝词」列）。
- **下一步**：`/prd`。

### 3.4 `/prd` —— 写正式 PRD

- **定位**：把确认的 feature brief 写成正式 PRD。
- **前置**：feature brief 存在且状态为「待写PRD」，否则阻塞。
- **用法**：`请基于 feature brief，用 /prd 产出 PRD`。
- **产出**：PRD → `./docs/features/<slug>/<summary>-prd-<日期>.md`。
- **下一步**：`/pd-review`。

### 3.5 `/pd-review` —— PRD 评审

- **定位**：七维评审（目标/范围/规则/状态异常/交接/验收/术语一致性，各 0–10 分），低于 8 分直接补文档而不是只提建议。
- **用法**：`请用 /pd-review 审查当前 PRD，直接补齐不足，并给出可交付结论`。
- **产出**：修订后的 PRD + pd-review-report。**结论=可交付**是进入 dev 阶段的门槛。

### 3.6 `/issue` —— 已上线功能的小变更

- **定位**：对已存在功能做局部优化（入口调整、阈值变化、文案、轻量修补），不走向全链。
- **用法**：`请用 /issue 整理"X 功能"的优化：<现状与打算>`。
- **产出**：change request → `./docs/features/<slug>/`。需要走开发流程的，变更单直接作为 `/tech-spec` 的需求输入（小变更路径，跳过 `/prd`、`/pd-review`）；小到不需拆票的，人直接手工改或作为 `/implement` 的单票输入。

### 3.7 `/tech-spec` —— 技术方案

- **定位**：把需求输入变成技术方案：模块/接口/数据/流程、**约束与假设分栏**（约束不可违，假设写清证伪后怎么调）、预定 TDD 接缝。需求输入二选一：标准路径为可交付 PRD；小变更路径为 `/issue` 的 change-request（不重走 `/prd`、`/pd-review`）。
- **前置**：`/setup-dev` 已跑；标准路径要求 PRD 可交付（pd-review 通过），小变更路径要求该 slug 下存在最新 change-request。
- **用法**：`请用 /tech-spec 基于当前 PRD 出技术方案`，或 `请用 /tech-spec 基于当前变更单出技术方案`。技术决策同样会被拷打并出共识摘要请你确认。若该 slug 下已有原型 verdict（`prototypes/`），其裁决结论自动进入方案输入边界；verdict 中的决策性片段（状态机 / schema 等）是 tech-spec 唯一允许内联的代码，收进模板「3.5 决策性片段」并标注来源。
- **产出**：tech-spec → `./dev/features/<slug>/<summary>-tech-spec-<日期>.md`，状态「已确认（待拆解）」。
- **下一步**：`/issue-split`。

### 3.8 `/issue-split` —— 拆票 + 门②

- **定位**：把 tech-spec 拆成垂直切片票（tracer bullet：每票纵向打通各层、独立可演示、声明阻塞边 DAG），DoD 全部写成「命令 + 期望输出」；**编号清单请你确认（门②）后才发布**。
- **用法**：`请用 /issue-split 拆解当前 tech-spec`。确认时可调粒度、调阻塞边、改验收命令。
- **产出**：票 → `./dev/features/<slug>/issues/issue-NNN-<短名>.md`（tracker=gitlab 时同步建 GitLab issue 并回写 IID）。
- **下一步（自动）**：门②通过后，主会话按 DAG 逐票派发——对每张 frontier 票由宿主 agent **自动新开一个独立对话**执行 `/implement issue-NNN`（提示词自包含：票 / tech-spec / agents-config 路径；机制宿主无关：Claude Code `Task`、ZCode `Agent`，Codex / WorkBuddy 用各自新开对话能力），对话模型默认用低一档的经济档；主会话派出后不停手，核对落盘产物、执行合并（一张收口再派下一张），监督到全部票合并完成。门②是执行段之前唯一的人工门。

### 3.9 `/implement` —— 实现、证据、MR（dev 主流程）

- **定位**：驱动一张 confirmed 票走完「编码 → 证据 → 评审 → MR ready」。通常由主会话以宿主新开独立对话派发；人也可直接在主会话跑 `/implement issue-001`。
- **前置**：票 `confirmed`；blocked-by 的票全部 `done`；仓库非 A 类。
- **用法**：`/implement issue-001`。九步自动推进：
  1. 校验（不过即阻塞，不动代码）
  2. `git worktree add .worktrees/issue-001 -b feat/<slug>-issue-001`（隔离，可多票并行）
  3. TDD：先失败测试再实现；每轮只跑质量门 **fast**（日常开发默认档）；命中红线（协议/密钥/改测试过关）立即停
  4. 自修复 ≤ max-fix-rounds 轮（默认 99），超限打回 `needs-human` 附完整轨迹
  5. 证据落盘：DoD 命令逐条实际执行，`evidence-issue-001.md`（命令+退出码+输出+commit hash），**不信自报**；交付前跑一次质量门 **full**（全量检查，不可跳过）
  6. push + **建 draft MR** → 以宿主新开的独立对话跑 `/ai-review`（默认 delegate：ocr 筛文件/解析规则，独立对话评审；local/ci 为可选模式）
  7. BLOCKER（critical/high）回修 → 重新评审 → 直至干净或轮次耗尽
  8. MR 转 ready（`glab mr update --ready`），正文=变更摘要/证据/评审结论/diff 统计；**合并由主会话执行**——被派发对话只到 ready 并结构化回报；主会话核对落盘产物后直接 `glab mr merge`（或本地 merge），票置 `done`、删分支、清 worktree
  9. 报告合并结果与遗留清单。**全程无需人审**；只有 `needs-human` / 阻塞 / 无法裁决时才停下找人
- **产出**：feature 分支、`impl/issue-001/`（impl-log + evidence）、draft→ready→已合并的 MR、票状态 `done`。
- **local tracker 模式**：产 MR 草案文件到 `mr/`，主会话执行本地合并命令。

### 3.10 `/ai-review` —— 独立评审（OCR 驱动）

- **定位**：用 open-code-review 评审固定点以来的 diff，解析成评审报告 + BLOCKER 清单。通常由 `/implement` 触发，也可单独用。
- **用法**：`/ai-review`（默认 main...HEAD）或 `/ai-review <commit/分支/tag>`。三模式（按 agents-config）：
  - `delegate`（**默认**）：无需 LLM 端点——ocr 只筛文件/解析规则，宿主 agent 内新开独立对话评审（默认低一档经济模型）
  - `local`：worktree 内 `ocr review --from main --to HEAD --background-file <票文件>`（票 DoD 作需求背景）
  - `ci`：拉 CI artifacts `.ocr/ocr-result.json` / MR discussions 解析
- **产出**：review-report → `reviews/`；BLOCKER = critical/high + 红线命中（红线复核独立做，ocr 的 exclude 管不住「不允许改」）。
- **纪律**：只评不改；评审必须独立实例（CI 天然独立；实现会话不得自评）；findings 原样入报告，不改写严重度。

### 3.11 `/review` —— 发布后复盘

- **定位**：一个 feature 全部票 done、通过验收并确认发布之后，把 `./docs/` 与 `./dev/` 两棵树的过程产物读成一份复盘：还原过程全景（阶段时间线、每票开发轮次、问题清单），抽取需求侧关键点（产品经理视角：需求文档阶段应加深思考的点）与开发侧关键点（导致重复修改的坑），并把经验分级沉淀。
- **前置**：该 slug 的票全部 `done`（否则阻塞，除非明确要求部分复盘）；发布事实由人口头确认（P4/P5 无落盘产物）。
- **用法**：`/review <feature-slug>`。
- **产出**：复盘报告 → `./docs/features/<slug>/<summary>-retro-<日期>.md`；经验分两级沉淀——项目级追加 `./docs/EXPERIENCE.md`（仿 GLOSSARY 的单例追加模式），通用级追加 `~/.pd-workflow/general-experience.md`（跨项目全局单例）；相似条目自动合并、复现计数，项目级条目复现 ≥2 个 feature 后晋升通用级。
- **纪律**：只读历史、只写三处（报告 + 两个经验库）；统计只认落盘文件，取不到标「缺失」，关键点必须挂证据。

### 3.12 `/prototype` —— 不可拷打问题的一次性原型

- **定位**：拷打（`/pd-plan` / `/prd` / `/tech-spec`）遇到靠对话问不出答案的问题时的出口：做一个一次性原型让人上手反应，把裁决落盘。一个原型只回答一个问题。
- **用法**：`请用 /prototype 验证：<问题>`（一般在拷打挂出 `待确认（需原型）` 后调用）。两条分支按问题自动选择：
  - `LOGIC`（这套规则 / 状态模型感觉对吗）：纯逻辑核（reducer / 状态机，可整体搬进真实实现）+ 单文件 HTML，双击可开、非开发者可点；guided walkthrough 覆盖快乐路径 / 尴尬边界 / 应非法操作——「等等，这不应该可能」正是要找的发现。
  - `UI`（这个交互 / 界面应该长什么样）：3 个结构迥异的变体（布局 / 信息层级 / 主操作不同，不是换颜色），有前端仓库时挂在现有路由 `?variant=` 后，生产构建隐藏切换条。
- **产出**：原型文件 + verdict → `./dev/features/<slug>/prototypes/<短名>/`；verdict 记录问题 / 场景或变体 / 裁决 / 决策性片段（可选）/ 回写建议。
- **下一步**：回到触发它的拷打继续（该题改标 `已确认`）；决策性片段由 `/tech-spec` 按「3.5」节收录。
- **纪律**：默认 IMPLEMENT_MODE（显式调用即授权）；只写 `prototypes/**`，不碰产品源码与上游文档；一次性、无持久化、不打磨。

### 3.13 `/handoff` —— 会话交接（不落盘）

- **定位**：把当前对话压缩成一份自包含的交接提示词，**直接输出在对话里**（包在一个代码块中），复制到新会话开头即可续接；不写任何文件。
- **用法**：`/handoff` 或 `/handoff <新会话的用途>`（带参数时按用途裁剪详略，无关旁支一笔带过）。
- **产出**：对话内的交接块，七节——目标 / 当前状态（含 feature-slug 与所处阶段）/ 已决事项 / 未决事项与坑 / 下一步 / 产物路径引用清单（类型匹配 + 最新日期版本，具体到文件名）/ 建议调用的 skill 与时机。产物正文不复制进交接，只引路径。
- **纪律**：不落盘（含临时目录）；API key / token / 密码 / 个人身份信息不进交接内容。

---

## 四、仓库维护者须知

- **改动流程**：改 `SKILL.md.tmpl` / `shared/fragments/` / `shared/templates/` → `bash skills/product-workflow/scripts/build-all.sh` 重新生成全部 SKILL.md（Node ≥ 22.6，脚本用 `--experimental-strip-types` 跑 TS）→ 分发（重跑 2.5 的 cp）。
- **新增 skill**：在 `skills/product-workflow/` 下建目录放 `SKILL.md.tmpl`（占位符只可用 `scripts/gen-skill-docs.ts` fragmentMap 里注册过的）；引用新模板必须先在 `shared/templates/` 建文件，构建时会校验。
- **SKILL.md 是生成物**：顶部有 AUTO-GENERATED 标记，不要直接编辑。
- **模板即时生效**：`shared/templates/*.md` 被 skill 运行时读取，改模板不需要重新 build。

## 五、边界与红线（务必读）

1. **A 类仓库**（飞行软件 / 涉密 / 型号数据）：`/implement` 拒跑，AI 仅只读辅助；涉密数据不进任何云端模型（含国产）。
2. **人工门只剩门②**：票清单确认是执行段之前唯一的人审检查点。之后派发、实现、评审、合并、按步推进全自动——评审通过后由主会话直接合并 MR，无需人审；只有被派发对话报 `needs-human`、阻塞、合并冲突无法自动解决等问题时，才停下与人确认。
3. **数据不出境**：默认形态（宿主新开对话派发 + delegate 评审）不依赖外部 LLM 端点；如启用 local / ci 评审，GLM（bigmodel.cn）、内网 GitLab、ocr 自托管，全链境内；本仓库 remote 若指向境外托管（如 GitHub），不得 push 公司数据。
4. **验证与发布未纳入**：当前版本止步于 MR 合并；上线前的分层验证、回滚预案属后续阶段，勿默认已存在。发布之后的复盘已纳入：票全部 done 且人确认发布后，用 `/review` 沉淀经验。
